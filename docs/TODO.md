# TODO

- Implement full Section 13 HUD fields from `rules.md` per hostile track:
  - type, speed, altitude, range, ETA, threat score, recommended counter.
- Add manual launch-lane strategy UI for attacker (16 site choices: 4 edges x 4 lanes).
- Add optional advanced-mode friendly-fire/IFF degradation model from `rules.md`.
- Tune persistent fire scaling and visual layering for best LCD readability.
- Add deterministic scenario seed controls for repeatable balancing runs.
- If EdgeProtect tools become available, validate full combined-image signing/program path.
